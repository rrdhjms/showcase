defmodule ShowcaseServerWeb.AcronymControllerTest do
  use ShowcaseServerWeb.ConnCase

  # alias ShowcaseServer.Acronyms
  # alias ShowcaseServer.Acronyms.Acronym

  # @create_attrs %{
  #   acronym: "some acronym"
  # }
  # @update_attrs %{
  #   acronym: "some updated acronym"
  # }
  # @invalid_attrs %{acronym: nil}

  # def fixture(:acronym) do
  #   {:ok, acronym} = Acronyms.create_acronym(@create_attrs)
  #   acronym
  # end

  # setup %{conn: conn} do
  #   {:ok, conn: put_req_header(conn, "accept", "application/json")}
  # end

  # describe "index" do
  #   test "lists all acronym", %{conn: conn} do
  #     conn = get(conn, Routes.acronym_path(conn, :index))
  #     assert json_response(conn, 200)["data"] == []
  #   end
  # end
end
