defmodule ShowcaseServerWeb.SpaceAgeControllerTest do
  use ShowcaseServerWeb.ConnCase

  # alias ShowcaseServer.SpaceAges
  # alias ShowcaseServer.SpaceAges.SpaceAge

  # @create_attrs %{
  #   age: 120.5
  # }
  # @update_attrs %{
  #   age: 456.7
  # }
  # @invalid_attrs %{age: nil}

  # def fixture(:space_age) do
  #   {:ok, space_age} = SpaceAges.create_space_age(@create_attrs)
  #   space_age
  # end

  # setup %{conn: conn} do
  #   {:ok, conn: put_req_header(conn, "accept", "application/json")}
  # end

  # describe "index" do
  #   test "lists all space_age", %{conn: conn} do
  #     conn = get(conn, Routes.space_age_path(conn, :index))
  #     assert json_response(conn, 200)["data"] == []
  #   end
  # end
end
