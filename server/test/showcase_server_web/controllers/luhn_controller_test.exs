defmodule ShowcaseServerWeb.LuhnControllerTest do
  use ShowcaseServerWeb.ConnCase

  # alias ShowcaseServer.Luhns
  # alias ShowcaseServer.Luhns.Luhn

  # @create_attrs %{
  #   number: 42
  # }
  # @update_attrs %{
  #   number: 43
  # }
  # @invalid_attrs %{number: nil}

  # def fixture(:luhn) do
  #   {:ok, luhn} = Luhns.create_luhn(@create_attrs)
  #   luhn
  # end

  # setup %{conn: conn} do
  #   {:ok, conn: put_req_header(conn, "accept", "application/json")}
  # end

  # describe "index" do
  #   test "lists all luhn", %{conn: conn} do
  #     conn = get(conn, Routes.luhn_path(conn, :index))
  #     assert json_response(conn, 200)["data"] == []
  #   end
  # end
end
