defmodule ShowcaseServerWeb.BobControllerTest do
  use ShowcaseServerWeb.ConnCase

  # alias ShowcaseServer.Bobs
  # alias ShowcaseServer.Bobs.Bob

  # @create_attrs %{
  #   sentence: "some sentence"
  # }
  # @update_attrs %{
  #   sentence: "some updated sentence"
  # }
  # @invalid_attrs %{sentence: nil}

  # def fixture(:bob) do
  #   {:ok, bob} = Bobs.create_bob(@create_attrs)
  #   bob
  # end

  # setup %{conn: conn} do
  #   {:ok, conn: put_req_header(conn, "accept", "application/json")}
  # end

  # describe "index" do
  #   test "lists all bob", %{conn: conn} do
  #     conn = get(conn, Routes.bob_path(conn, :index))
  #     assert json_response(conn, 200)["data"] == []
  #   end
  # end
end
